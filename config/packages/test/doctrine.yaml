doctrine:
  dbal:
    default_connection: default
    connections:
      default:
        driver: pdo_sqlite
        url: 'sqlite:///%kernel.project_dir%/var/test_db.sqlite'
        charset: UTF8
        default_table_options:
          charset: utf8
          collate: utf8_unicode_ci
