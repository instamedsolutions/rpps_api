doctrine:
  dbal:
    default_connection: default
    connections:
      default:
        driver: '%env(DATABASE_ENGINE)%'
        url: '%env(DATABASE_URL)%'
        charset: UTF8
        default_table_options:
          charset: utf8
          collate: utf8_unicode_ci
